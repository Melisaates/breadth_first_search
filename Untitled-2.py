 '''for vertex in range(0, v):
        for u in G[vertex]:
            if u != s:
                u = Point(colors.get("white"), math.inf, None)'''



                '''def generate_neighbor_edges(graph,vertex):
    

    edges = []
    for node in graph:
        #for neighbour in graph[node]:
        for value in graph.items():
            if node==vertex:
                print("++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++---")
                edges.append(neighbour)
                
    edges=all_vertices(edges)            
    return edges

def all_vertices(graph):
    ret=[]
    for key in graph.items():
        key=Point(colors.get("white"), math.inf, None)
        ret.append(key)

    return ret'''

    '''for key,value in graph.items():
        if key==vertex:
            print("++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++---")
            edges.append(value)'''


            '''for key in G.items():
        if key!=s:
            key=Point(colors.get("white"),math.inf, None)
    '''

    
'''def do_obje_vertices(graph):
    

    ret=[]
    for key in graph.items():
        key=Point(colors.get("white"), math.inf, None)
        ret.append(key)
    print("2222222222222222222222222222")
    return ret

'''